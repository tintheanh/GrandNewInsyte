const postsPerBatch = 20;
const commentsPerBatch = 20;
const repliesPerBatch = 20;
const userPostsPerBatch = 20;
const userResultsPerBatch = 10;
const userTagResultsPerBatch = 5;
const placeResultsPerBatch = 10;
const milesRadius = 5;
const milesRadiusForCategorySearch = 10;

export * from './firebase';
export {
  postsPerBatch,
  commentsPerBatch,
  repliesPerBatch,
  userPostsPerBatch,
  userResultsPerBatch,
  userTagResultsPerBatch,
  placeResultsPerBatch,
  milesRadius,
  milesRadiusForCategorySearch,
};
